k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.
k
## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.



q
q
The important status line was:

```text
Active: active (running)# Day 6 — Linux Services and Logs

## Commands Practiced

- service --status-all
- systemctl
- service nginx status
- service nginx start
- service nginx stop
- service nginx restart
- nginx -v
- curl localhost
- ls /var/log/nginx
- tail
- tail -f

## What I learned

Today I learned how Linux services run in the background and how to check service status and logs.

## Why this matters for DevOps

DevOps engineers manage services such as Nginx, Docker, SSH, databases, backend applications, and monitoring agents.

When a service is not working, the first things to check are:

- service status
- logs
- ports
- recent errors

## Command Meanings

service nginx status = check if Nginx is running  
service nginx start = start Nginx  
service nginx stop = stop Nginx  
service nginx restart = restart Nginx  
nginx -v = check Nginx version  
curl localhost = test the local web server  
tail file = show the last lines of a file  
tail -f file = watch a log file live  

## Important Result

Nginx was active and running.

The important status line was:

```text
Active: active (running)service nginx status = check if Nginx is running
curl localhost = test the web server
tail access.log = see web requests
tail -f access.log = watch requests live
CTRL + C = stop watching live logs
