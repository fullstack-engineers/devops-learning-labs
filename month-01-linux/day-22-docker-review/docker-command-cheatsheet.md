# Day 22 Review — Docker Review

## Today I reviewed

- Docker images
- Docker containers
- Dockerfile
- Docker build
- Docker run
- Docker logs
- Docker exec
- Docker volumes
- Docker networks
- Docker Compose

## Main Docker Commands I Know

- docker --version
- docker images
- docker pull
- docker build
- docker run
- docker ps
- docker ps -a
- docker logs
- docker exec
- docker stop
- docker rm
- docker rmi
- docker volume ls
- docker network ls
- docker compose up -d
- docker compose down

## Main Docker Concepts I Understand

Image = application template/package

Container = running instance of an image

Dockerfile = instructions to build an image

Volume = persistent data storage

Network = container communication

Compose = run containers from docker-compose.yml

## What I learned this week

I learned how to run containers, build a custom image, use volumes, create networks, and run a container using Docker Compose.

## Next Step

Next I will start Git and GitHub deeper practice for DevOps.
